<div>
  <!--<h1><%= @user.name %></h1>-->
  <!--<section class="stats">-->
  <!--    <%= render 'devise/shared/stats' %>-->
  <!--</section>-->
  <!--<div>-->
    <h3 class="users-title"><%= @title %></h3>
    <% if @users.any? %>
      <ul class="users-follow">
        <% @users.each do |user| %>
          <li class="users-follow__li">
            <%= image_tag avatar_url(user), class: "post-profile-icon" %>
            <%= link_to user.name, user_path(user) %>
          </li>
        <% end %>
      </ul>
    <% end %>
  </div>
</div>